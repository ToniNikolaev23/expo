import { Text, View, StyleSheet } from "react-native";

const Page = () => {
  return (
    <View>
      <Text>Favorites</Text>
    </View>
  );
};

export default Page;
const styles = StyleSheet.create({});
